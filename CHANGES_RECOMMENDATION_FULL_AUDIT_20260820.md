# 추천 로직·카테고리 데이터 전수 검수 (2026-08-20)

## 변경 목표

- 전역 `선택 조건 정확일치` / 전역 `대체 추천` 상태 문구 제거
- 결과는 항상 5개 고정
- 첫 화면은 같은 핵심 식재료를 가능한 한 1개만 노출
- `다른 추천 보기`에서는 이전 화면에서 숨긴 동일 식재료 변형을 화면당 최대 2개까지 허용
- 결과는 최종 추천 점수 내림차순으로 표시
- 대체 추천은 항목별 빨간 테두리로만 구분하고, 실제 불일치 사유만 짧게 표시
- 맵기는 대체추천 판정에서 제외하고 감점만 수행
- 제철은 취향보다 낮은 보너스로 반영

## 최종 점수 구조

취향 점수 최대 100점:

1. 날것/익힘 40점
2. 주재료 30점
3. 국물 20점
4. 맵기 10점

제철 보너스:

- 선택 월 정확 일치 +12
- ±1개월 +6
- ±2개월 +3
- 그 외 +0

따라서 취향(최대 100)이 제철(최대 +12)보다 우선한다. 제철 불일치는 대체 추천 사유가 아니다.

같은 화면에서 두 번째 동일 식재료가 허용되는 경우 다양성 패널티 -15점을 적용한다. 첫 화면은 가능한 한 식재료당 1개, 새로고침 화면은 최대 2개다.

## 대체 추천 판정

대체 추천 여부는 다음 핵심 조건만 본다.

- 날것/익힘
- 주재료
- 국물

맵기는 대체 추천 판정에 사용하지 않는다. 선택 월도 대체 추천 판정에 사용하지 않는다.

정상 추천이 5개보다 적을 때, 핵심 조건을 일부 놓친 후보 중 최종 점수가 높은 순으로 부족한 자리를 채운다. 대체 추천 카드만 빨간 테두리로 표시하고, `날것/익힘 조건 불일치`, `주재료 조건 불일치`, `국물 조건 불일치`처럼 실제 사유만 표시한다.

## 데이터 분류 오류 수정

내부 296개 음식 데이터를 규칙 기반으로 재검수하면서 실제 분류 오류를 수정했다.

주요 수정 예:

- `죽순`의 `죽`을 죽(粥)으로 잘못 읽어 국물 있음으로 분류하던 부분 수정
- `우리밀 열무비빔국수` / `우리밀 비빔닭국수` 등 비빔국수류를 국물 없음으로 수정
- `낙지덮밥`, `낙지돌솥비빔밥`, `산낙지구이`, `산낙지전`, `산낙지전골` 등 조리된 낙지 요리가 날것으로 남아 있던 분류 수정
- `마른우럭지리탕` 국물 있음 수정
- 복합 재료 메뉴의 대표 카테고리 일부 보정

현재 수정본에서는 25개 음식 레코드의 취향 라벨을 직접 보정했고, 향후 데이터 재생성 시 같은 오류가 재발하지 않도록 `src/taste/rules.py`의 판정 규칙도 함께 수정했다.

## 전체 조합 자동 검수

카테고리 조합 1,152건을 전체 실행했다.

- 맵기 4단계
- 국물 3단계
- 날것/익힘 2단계
- 주재료 4종(해산물/육류/채소/상관없음)
- 12개월

검수 결과:

- 결과가 5개 미만인 조합: 0건
- 최종 추천 점수가 음수인 조합: 0건
- 대체 추천인데 사유가 없는 항목: 0건
- 정상 추천인데 대체 사유가 붙는 항목: 0건
- 첫 화면에서 같은 `ingredient`가 2건 이상 나온 조합: 0건
- `다른 추천 보기`에서 5개 미만인 조합: 0건
- 새로고침 한 화면에서 동일 `ingredient`가 3건 이상인 조합: 0건
- 첫 새로고침에서 직전 화면 foodId가 다시 나온 조합: 0건

## 사용자 제보 조건 재현 테스트

### 약간 매움 / 국물 없이 / 익힌 것 / 육류 / 3월

취향 핵심 조건을 만족하는 메뉴는 정상 추천으로 표시된다. 제철 월이 정확히 맞지 않는다는 이유만으로 대체 추천으로 바뀌지 않는다.

### 아주 매움 / 국물 있게 / 날것 O / 채소

현재 데이터에는 핵심 조건을 모두 만족하는 후보가 매우 부족하므로 점수가 가장 가까운 대체 추천을 5개까지 채운다. 잘못된 `우리밀 열무비빔국수` 국물 라벨은 수정되어 이전과 같은 반복 원인을 제거했다.

### 안 매움 / 국물 없이 / 날것 O / 해산물 / 1월

첫 화면의 핵심 식재료는 낙지·굴·홍어·삼치·멍게처럼 서로 다른 값으로 구성된다. 낙지 계열이 첫 화면을 여러 칸 차지하지 않는다.

### 안 매움 / 국물 없이 / 날것 O / 해산물 / 5~10월

제철 보너스를 강화해 월별 상위 결과가 실제로 달라진다. 검수 시 주요 식재료 구성은 다음처럼 변했다.

- 5월: 농어 / 병어 / 붕장어 / 삼치 / 우럭
- 6월: 민어 / 병어 / 붕장어 / 전복 / 갈치
- 7월: 갈치 / 농어 / 민어 / 붕장어 / 전복
- 8월: 갈치 / 민어 / 병어 / 붕장어 / 전복
- 9월: 갈치 / 낙지 / 전어 / 꽃게 / 민어
- 10월: 낙지 / 삼치 / 전어 / 꽃게 / 갈치

## 검수 범위의 한계

이번 검수는 프로젝트 내부 데이터 296개와 현재 분류 규칙에 대한 전수 일관성 검수다. 모든 메뉴의 실제 조리법·지역 전통을 외부 출처와 하나씩 대조한 사실 검증을 의미하지는 않는다. 다만 사용자가 제보한 오류와 규칙상 명백한 모순은 데이터와 재생성 규칙 양쪽에서 보정했다.
