# UI / 추천 다양성 수정 (2026-08-19)

- 최신 버전을 기준으로 수정.
- AI 자연어 추천 패널 우측 상단에 `카테고리 선택` 버튼 추가 (`/taste/conditions`).
- 추천 전 지도에는 전남 음식특화거리를 표시하고, 추천 후에는 거리 마커를 숨김.
- 추천 음식점 위치는 음식 아이콘 대신 작은 점 마커로 표시.
- 전남 지도 초기 화면을 `[34.72, 126.72], zoom 9`로 확대.
- 자연어 추천 결과에 `다른 추천 보기` 추가. 현재 추천 food ID를 API에 제외 목록으로 전달해 같은 취향의 다른 후보를 우선 추천.
- 카테고리 결과 화면에도 `다른 추천` 추가. 동일 취향 조건에 새 seed를 적용해 추천 조합을 변경.
- 실제 음식 사진 거리 아이콘은 현재 street 데이터에 이미지 URL/사진 필드가 없어 미적용. 기존 OpenMoji 기반 거리 아이콘 유지.
